services:
  backend:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${CORVOS_GPU_DRIVER:-nvidia}
              count: ${CORVOS_GPU_COUNT:-1}
              capabilities:
                - gpu

  celery_worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${CORVOS_GPU_DRIVER:-nvidia}
              count: ${CORVOS_GPU_COUNT:-1}
              capabilities:
                - gpu

  celery_beat:
    deploy:
      resources:
        reservations:
          devices:
            - driver: ${CORVOS_GPU_DRIVER:-nvidia}
              count: ${CORVOS_GPU_COUNT:-1}
              capabilities:
                - gpu
