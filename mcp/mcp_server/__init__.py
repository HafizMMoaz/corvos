"""Corvos MCP server.

Exposes Corvos's scrapers, knowledge base, and workspaces to MCP clients
(Claude Code, Cursor, Claude Desktop). Connects to a Corvos backend over its
REST API, authenticating with a Corvos API key.
"""

__version__ = "0.1.0"
